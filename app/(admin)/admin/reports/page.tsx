import ManageReportsClient from './ManageReportsClient';

export const metadata = {
  title: 'Manage Reports',
};

const ManageReportsPage = () => {
  return <ManageReportsClient />;
};

export default ManageReportsPage;
